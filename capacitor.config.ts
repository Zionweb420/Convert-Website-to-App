import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zionwebcreate',
  appName: 'Zionweb Create',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
